# electron-builder 兼容：让 pnpm 使用扁平化 node_modules（非符号链接）
# 否则 electron-builder 无法正确收集 pnpm .pnpm store 里的生产依赖，
# 导致打包后运行时 "Cannot find module"（如 ms、debug 等）。
# 修改后需删除 node_modules 并重新 pnpm install 生效。
node-linker=hoisted
