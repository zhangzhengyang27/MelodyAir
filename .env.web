# Web 构建专用环境（dev:web / build:web 共用，通过 --mode web 加载）
# Electron 桌面构建不读取此文件；桌面端的线上 API 地址由 .env.production（electron-vite build）注入
# 路由用 history 模式（部署端 Nginx 需 try_files 回退 /index.html）
VITE_ROUTER_MODE=history
# 线上 API 地址仅在 build:web 命令行注入（dev 时保持 localhost 默认，便于本地调试）
