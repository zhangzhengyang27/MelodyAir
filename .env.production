# Electron 桌面生产构建专用（electron-vite build 默认 mode=production 自动加载）
# 注入线上 API 地址，使桌面端默认请求线上后端，而不是开发用默认值 http://localhost:3001
#
# 不影响其他构建路径：
# - electron-vite dev 是 mode=development，不读此文件（本地调试仍指向 localhost 后端）
# - build:web 用 --mode web，只读 .env / .env.web，不读此文件（线上地址由 build:web 命令行注入）
#
# 用文件而不是像 build:web 那样在命令行前置环境变量，是因为 release CI 的 win-x64
# 跑在 Windows runner 上，`VAR=value command` 的 shell 前缀语法在 cmd 下不可用
VITE_API_BASE=https://music-api.zhangzhengyang.com
